# Local configuration -- contains server.token, never commit it.
# Start from config.example.toml instead.
config.toml

# Internal audit and planning notes -- working documents, not part of the
# shipped project. The design docs that describe what was built do get
# committed; these describe what might be.
docs/product-audit-*.md

# Recorded history
*.db
*.db-wal
*.db-shm

__pycache__/
*.pyc
.DS_Store

# macOS app: project.yml is the source of truth, the .xcodeproj is generated
# by `xcodegen generate`.
macos/PowermonMenuBar.xcodeproj/
macos/PowermonMenuBar/Resources/Info.plist
macos/build/
macos/*.xcworkspace/
xcuserdata/
